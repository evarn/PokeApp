import React, {useState} from 'react';
import {SafeAreaView, ScrollView, StyleSheet, Text, View} from 'react-native';

const ComparePage = () => {
  return (
    <View>
      <Text style={styles.Text}>Compare Page</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  Text: {
    textAlign: 'center',
    fontSize: 36,
    padding: 0,
    color: 'black',
  },
});

export default ComparePage;
